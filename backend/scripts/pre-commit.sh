#!/bin/sh
# TODO(backend-team): replace with real pre-commit checks.
# Keep them FAST -- a slow pre-commit hook is a hook people disable.
echo "   backend: no pre-commit checks configured yet"
exit 0
