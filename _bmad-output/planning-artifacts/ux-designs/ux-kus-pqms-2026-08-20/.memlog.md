---
topic: N-PQMS Issue & Signal Management (ISM) — UX spines (DESIGN.md + EXPERIENCE.md), grounded in synced Kia design system
updated: 2026-08-24T20:20
---

- (event) Wrote DESIGN.md (frontmatter tokens mirrored from synced Kia design system: colors/typography/rounded/spacing/components + 8 body sections) and EXPERIENCE.md (Foundation, IA, Voice&Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, 5 Key Flows, Responsive, Inspiration). Fast-path autonomous from synced design + PRD + SE-role spec
- (assumption) DESIGN.md treats design-system tokens as canonical over SE-prototype page literals (app bg #FAFBFC vs #F6F8FA; text #1A1A1A vs #1A2430; card radius xl 12px vs prototype 14-16px); WCAG 2.1 AA target; Attention-Required criteria non-score (from PRD open questions). Confirm.
- (decision) Scope in spines: canonical roles(SE read/propose, ASM+PQM override/approve, Administrator) & lifecycle & one-hue-per-status applied; SeverityIndicator/SeverityBar marked reference-only (scoring out); QIR present only as issue->QIR hand-off action (no QIR screens); TSB no surface; default list sort Date Reported desc
- (event) UX spines finalized (status: final). DESIGN.md + EXPERIENCE.md polished; concrete shadow values added to Elevation. Open [ASSUMPTION]s remain: design-system tokens vs SE-prototype literals (app bg/text/card radius), WCAG 2.1 AA target.
- (event) V4-V5 'Hand off to Claude Code' sync: pulled 'ISM + QIR SE Role - P:C.dc.html' from project 6a717b29 (partial, 256KiB cap, encoding-safe) into design-source/prototypes; lucide-local.js retrieved but not mirrored (open-source helper, documented in DESIGN.md); _ds design system shared with V2-V3 (already synced); _ds_bundle.js skipped. QIR stays out of scope (reference only). Recorded in design-source/SYNC.md
- (event) ISM alignment pass (Update mode) against the COMPLETE V4-V5 export from Kia N-PQMS V4-V5.zip (1,837,340 bytes) — supersedes the earlier partial 256KiB-capped MCP pull. Extracted prototype behaviour for 8 areas: bulk action bar, Edit Issue, summary filters, Issue Entry model code/year + linking, Investigation segments, Communication composer + Resolution, Activity Timeline, plus residual diff items. Written to ISM-ALIGNMENT-2026-08-24.md
- (decision) Edit Issue is IN-TAB, not a modal and not a separate route: startEdit() sets {tab:'overview', editMode:true} and showWorkflowGrid excludes editMode+overview, replacing the Issue Detail tab content with the full pre-populated Issue Entry form. Answers the open question about popup vs route
- (override) Reverses ALIGNMENT-AUDIT-2026-08-22 correction #3: Part Requests belong under INVESTIGATION (one of two invFilterOpts segments), not Resolution. The audit inferred Resolution from _tabAlias, but that map exists to resolve legacy deep links, not to express current IA. PENDING USER CONFIRMATION
- (decision) Issue Priority scoring (A/B/C, 17-item matrix, thresholds >=26/>=11) coexists with the standing 'severity scoring out of scope' decision — three distinct scales: Issue Priority, QIR ratingOf() 70/40, and the barred severity score. Spines must state this so they are not conflated
- (decision) Bulk action bar CONFIRMED from template: floating dark pill, position:fixed left:50% bottom:30px translateX(-50%), z-index 150, background var(--kia-midnight), radius 16, padding 8px 10px, shadow 0 18px 44px rgba(5,20,31,.34). Circular white count badge 28px. Action buttons h40 radius10 transparent #fff 14.5px/600. Divider 1px x 26px rgba(255,255,255,.16). Menus open UPWARD (bottom:calc(100%+12px), w210, white, radius12). Scrim position:fixed inset:0 z-index 140. Ladder: scrim 140 < bar 150 < menu 160
- (event) Noted a real prototype bug: clearFilters() assigns sortKey twice ('registered' then 'priority'); last write wins, so clearing filters silently re-sorts by priority. Decide deliberately rather than porting verbatim
- (decision) CONFIRMED by Prisilla: Part Requests live in the Investigation tab of Issue Detail alongside Investigation Activities, per V4-V5. ALIGNMENT-AUDIT-2026-08-22 correction #3 (which moved them to Resolution from the _tabAlias legacy-deeplink map) is reverted
- (decision) Severity scoring stays OUT OF SCOPE — no spine rewording, no severity score/SeverityBar/SeverityIndicator work. Read as dismissing the documentation conflict, not as removing the already-delivered Issue Priority tab (flagged back to the user)
- (decision) clearFilters() duplicate sortKey assignment is a prototype BUG — FIX, do not port. Clearing filters must restore the default sort (registered desc), not silently re-sort by priority
- (event) Build order agreed, starting area 4: Issue Entry multi-select model codes with per-code model-year checkbox rows, plus issue linking (search / select / link / unlink)
- (change) EXPERIENCE.md updated: Part Requests moved back to Investigation (audit #3 reversed, noted in the audit doc itself so the artifacts cannot contradict); Model code & year and Issue linking added to Component Patterns with the union rule, idempotent+reciprocal linking, and the anchor-code rule; Flow 2 step 2 re-pointed at Investigation. updated: 2026-08-24
- (event) Area 4 BUILT: src/data/modelCodes.ts (shared MC_MASTER, lifted out of CreateIssueScreen), ModelCodeYearPicker.tsx, LinkIssuesSection.tsx; CreateIssueScreen rewired to multi-code selection + links; store NewIssueInput gained modelCodes/yearsByCode/linkedIssueIds with reciprocal linking on create. clearFilters sort bug fixed. Adherence ratchet 638 -> 662, rationale documented in frontend/README.md
