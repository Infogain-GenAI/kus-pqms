---
updated: 2026-08-23T18:05
---

- (outcome) Full-window fidelity party (Vijay): found the prototype is fluid max-1800/40px, not centered-1200 — rebuilt the shared rail, fixed ws right rail to 340px, added source-evidence sub-card + in-card pagination; verified matched at 1920 AND 1280.
- (moment by architect) Winston caught that 1280−80=1200 made Amelia's container cap look right by pure coincidence — 'coincidence dressed as correctness' is now a house phrase.
- (dynamic) Sally pixel-notes every card; Amelia answers 'show me a screenshot' before theory; Murat insists on regression passes at the old width. Winston and Amelia agreed once and both found it suspicious.
- (outcome) Round 2 with Vijay: rendered the LIVE .dc.html prototype locally (its own runtime) — it disagreed with the flattened export: no Relationship column by default, 6th KPI is CLOSED, title col fixed ~380 w/ equal-share tails. Fixed all three; /issues now mirrors the live design at 1920.
- (callback) Sally's line: 'the flattened export lied to us — always render the source.' Murat now distrusts all exports on principle.
- (outcome) Round 3: scraped the live prototype's rendered dataset (7 mine + 28 all) and reseeded the app with it — HV-260101 hero, warranty evidence card, 6-unread bell. /issues is now a row-for-row twin; only carve-outs differ (canonical statuses, real date sort vs proto's broken sort).
- (moment) The prototype's list header claims 'ISSUE DATE ↓' but renders raw dataset order — the room voted to match the intent, not the bug. Amelia: 'we match designs, not typos.'
- (outcome) Round 4 with Vijay: read seedIssues()/_seedIssuesNorm()/ovClassify()/issueMDY() straight from the .dc.html source instead of scraping rendered output — found the true row count (33, not 28), that owner and assignee are genuinely different fields (CREATOR map overrides owner after assignee is computed), and MC_MASTER for Create Issue's model codes. Caught and fixed a self-introduced bug along the way: 6 relative-labeled issues plus the app's own NOW anchor had drifted to Aug 2026 instead of the source's hardcoded _todayBase() = Jul 9 2026 — re-verified row-for-row against a fresh dc-compare.mjs capture.
- (outcome) Round 5 with Vijay: fixed Overview's remaining icon drift (roleChip, lastLogin clock, empty-state icon), then swept the whole Issue Management surface — List, Create, and all 5 Workspace tabs — for icon/label/font fidelity. Root-caused a repo-wide label-color miss (`--text-muted` used where the source's dominant micro-label idiom, 178 vs 61 occurrences, says `--text-disabled`) and fixed it once at the shared `fieldLabel`/`CardHead`/`Select` chevron instead of touching call sites one by one.
- (moment by architect) Winston's turn to catch it this time: the first TagChip fix stripped uppercase globally off one report that Disposition/Related-QIR pills render sentence-case in the source. Turned out the source has TWO pill idioms — a big sentence-case `statusStyle` pill and a small uppercase `catStyle` tag — and TagChip had been correctly built from the second one for five other call sites (EWS flagged, file count, History's Lifecycle/Audit Log). Same shape of mistake as the 1280−80 coincidence: fixing what you're looking at, breaking what you're not. Redone as a `style` opt-out on just the two confirmed call sites.
- (callback) That fix surfaced a second bug for free: History's AUDIT LOG badge had been falling back to neutral gray because classify()'s color branch only ever special-cased LIFECYCLE — source's catStyle puts Audit Log in purple (#6B4EDB/#EEEBFB). Sally: 'show me a populated issue, not the empty one' — found HV-260101 had six history rows and made the miss obvious on screen. Murat asked for the regression shot on Dashboard's badges anyway; came back clean.
- (outcome) Round 6 with Vijay — the v2 sync prompt, whole-surface pass: closed the Model Code data gap (name→code map + ISSUE_MULTI arrays: 'SV / 3 Models / 2 Models' row-for-row), added the boot-time date-anchor assertion, wired the DS's own _adherence ruleset into every build (ESLint runs it because oxlint can't execute no-restricted-syntax — an adherence file that won't run is just documentation with a filename), rebuilt Admin against a first-ever dc-admin capture, built the missing bell dropdown with the proto's verbatim NOTIFS() six, and replaced the list's inline filter strip + columns popover with the proto's 452px drawers.
- (moment by architect) The admin screen is unreachable in the SE proto's own nav — its adminMenuItems are dead vals no template ever references. Winston's probe ladder: window handle (nothing), data-props startScreen (ignored), then patch the constructor literal itself — third rung held, and dc-compare now regenerates the patched copy on every run. 'If the UI won't take you there, the source will.'
- (dynamic) Sally's string sweep came back 231 rows deep (128 match / 40 mismatch) and caught the proto using THREE ASCII DOTS in 'Search by keyword...' where we'd typed a real ellipsis — one code point, logged as a mismatch, fixed. Amelia found the History tab grouping against the real clock instead of the NOW anchor ('nothing has been Today since July'), and Murat made the drawer Save-changes subtitle earn itself: the Manage-Related modal now genuinely batches on Save, so the copy stopped being aspirational.
- (outcome) Round 7 with Vijay: he looked at the list KPIs (IN REVIEW / PENDING APPROVAL / ESCALATED) and asked why they don't match the mockup — the Phase-1 'canonical 8-status' decision, overturned by its owner. Investigation showed the proto's 7-status map shares our KEYS (review, escalated…) and we'd only ever diverged on labels plus two merged statuses; exactly 2 of 33 seed rows were wrong (ST-260002 topissue, BD-260012 outofscope). Adopted verbatim: Investigating/QIR/Top Issue/NASO everywhere, KPI strip now flame-and-all identical to dc-list, dashboard rebuilt onto the proto's five lifecycle stages.
- (moment) 'Pending Approval' turned out to be a status we INVENTED — the proto has no such state. The propose→approve flow survives, but as proposal metadata riding the real status, not a fake status. Winston: 'we modeled our process into their vocabulary; the fix was to stop.' The adherence ratchet also earned its keep: eight new proto-literal hexes tripped --max-warnings 615 and failed the build until re-pinned at 623, on purpose.
