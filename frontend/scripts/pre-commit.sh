#!/bin/sh
# TODO(frontend-team): replace with real pre-commit checks.
# Keep them FAST -- a slow pre-commit hook is a hook people disable.
echo "   frontend: no pre-commit checks configured yet"
exit 0
