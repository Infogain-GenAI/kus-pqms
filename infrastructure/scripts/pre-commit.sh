#!/bin/sh
# TODO(devops-team): replace with real pre-commit checks.
# Keep them FAST -- a slow pre-commit hook is a hook people disable.
echo "   infrastructure: no pre-commit checks configured yet"
exit 0
